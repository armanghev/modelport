---
title: Usage
---

<Callout type="warn" title="Placeholder">
There is no dedicated usage API endpoint in the current backend.
</Callout>

Usage data does exist internally and is exposed through analytics endpoints such as:

- `GET /analytics/overview`
- `GET /analytics/requests`
- `GET /analytics/models`
- `GET /analytics/costs`

If a dedicated usage endpoint is added later, this page should be replaced with that contract.
