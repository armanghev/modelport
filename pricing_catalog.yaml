# Local-only pricing defaults. Hosted-provider rates come from the LiteLLM catalog
# (see backend/app/pricing/catalog_import.py) and OpenRouter's own API at seed time.

catalog:
  ollama:
    "*":
      input_per_1m_usd: 0.0
      output_per_1m_usd: 0.0
