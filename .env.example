MODELPORT_TOKEN=dev-modelport-token
MODELPORT_DASHBOARD_TOKEN=dev-dashboard-token
# Keep enabled unless the dashboard and all /admin and /analytics routes are
# protected by another trusted access layer. Disabling it makes those APIs public.
MODELPORT_DASHBOARD_AUTH_ENABLED=true
# Required for encrypted DB credentials (see backend/app/security.py)
PROXY_ENCRYPTION_KEY=
OPENAI_API_KEY=
ANTHROPIC_API_KEY=
GEMINI_API_KEY=
OPENROUTER_API_KEY=
